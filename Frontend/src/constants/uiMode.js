// Controls whether the entity Create/Edit forms open inside a modal from the
// Query* list pages, or whether the list navigates to the standalone full-page
// routes instead. Flip this single flag to switch the whole app between modes.
export const ENTITY_FORMS_AS_MODAL = true
